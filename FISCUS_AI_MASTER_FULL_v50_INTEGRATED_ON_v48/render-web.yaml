services:
  - type: web
    name: fiscus-web
    env: node
    rootDir: apps/web
    plan: starter
    buildCommand: npm ci && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          name: fiscus-api
          type: web
          property: host
